{% extends 'AppBundle::base.html.twig' %}

{% block layout %}
    <h1>Maladie</h1>

    <div class="table-responsive">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maladie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ maladie.nom }}</td>
            </tr>
            <tr>
                <th>Traitement</th>
                <td>{{ maladie.traitement }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <ul>
        <li>
            <a href="{{ path('maladie_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('maladie_edit', { 'id': maladie.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
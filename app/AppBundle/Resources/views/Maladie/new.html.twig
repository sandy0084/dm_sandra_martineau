{% extends 'base.html.twig' %}

{% block layout %}
    <h1>Création d'une Maladie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('maladie_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}